import React from "react";

const App: React.FC = () => {
    return (
        <div>
            <h1>Hello, TypeScript with React and Webpack!</h1>
            <p>This is your TypeScript-based web app.</p>
        </div>
    );
};

export default App;
